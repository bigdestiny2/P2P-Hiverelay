services:
  blindspark:
    image: ghcr.io/bigdestiny2/p2p-hiverelay:0.26.0-rc.4@sha256:0fb632fd2f479e3853d866cf840ae1b24922a2a8b4d1e9ed772b0a859bd15f41
    command:
      - start
      - --storage
      - /data
      - --port
      - "9100"
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      - HOME=/data
      - HIVERELAY_API_KEY=${HIVERELAY_API_KEY}
      - HIVERELAY_ACCEPT_MODE=review
      - HIVERELAY_MAX_STORAGE=10GB
      - HIVERELAY_LOG_LEVEL=info
      - HIVERELAY_UI_SIMPLE=true
      - HIVERELAY_UI_EXPOSE_TOKEN=true
    x-runtipi:
      internal_port: 9100
      is_main: true

x-runtipi:
  schema_version: 2
