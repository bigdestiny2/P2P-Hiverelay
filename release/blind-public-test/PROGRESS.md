# PROGRESS — release-and-appliances (public-test artifact set)

Run: hiverelay-vnext-direct-https-public-test-storage-first-20260724t110740z
Lane: release-and-appliances · source: 447bbbf6fa17f8f4629a4a427e151937d06e29b4 (accepted, tree f21cc0d13e0c3dbb612c3e36f4bbe655affd2c23)
Release identity: 1.0.0-rc.1.public-test.1 · claim boundary LIVE_PUBLIC_TEST_ONLY · readiness stays zero · nothing signed/pushed/deployed

## Window 1 (2026-07-24T18:29–19:10Z) — COMPLETE

Built/verified:
- Split `Dockerfile.blind-edge` / `Dockerfile.blind-daemon` from `Dockerfile.blind`:
  toolchain `node:22-bookworm@sha256:5647be70…` + runtime `node:22-bookworm-slim@sha256:53ada149…`
  (both digest-pinned, multi-arch index digests); **no apt-get anywhere** (Debian
  inputs are exactly the pinned base contents); exact revision/build-id/component/
  bundle/claim-boundary labels; `SOURCE_DATE_EPOCH=1784907855` (= commit timestamp).
- **CA-roots defect fixed per relock verdict**: pinned slim base ships NO CA bundle
  (verified); Edge runtime now copies the pinned toolchain stage's Debian
  ca-certificates store and sets SSL_CERT_FILE. Smoke: default CAs=145, system CAs=284.
- Multi-arch builds (linux/amd64+linux/arm64) ×2 clean rounds each via buildx,
  mode=max provenance, OCI archives in `release/blind-public-test/oci-archives/`.
  **Platform manifests byte-identical across clean builds** (edge amd64
  65751c8f…/arm64 ebb7f2f9…; daemon amd64 b4f90134…/arm64 41f474a6…). Image indexes
  differ only in attestation entries (provenance timestamps), verified.
- Provenance attestations verified: SLSA v0.2, subjects match platform manifests,
  materials pin dockerfile frontend + both base images by digest, source revision
  build-arg present. Saved under `release/blind-public-test/provenance/`.
- SBOMs: syft v1.49.0 (digest-pinned image), SPDX-2.3 per image per platform
  (300 packages each), digests recorded.
- Browser control: regenerated v3 artifact twice from accepted source →
  byte-identical (`blind-client-control-v3.mjs` sha256
  874afd4a1927d4df2f0b439c1ecb72679de7eadde91ab256a330d85378e98744), `--check` passes,
  composition unit test 1/1. **Finding: committed v3 artifact at 447bbbf was stale**
  (built from node_modules with root compact-encoding 3.3.0 instead of locked nested
  2.19.2 under packages/blind-protocol); regenerated bytes are lockfile-faithful and
  committed separately. Browser bytes are NOT in either relay image (verified 0 files).
- Smoke: both images fail closed without signed topology/config (edge: missing
  launch-topology env error; daemon: BLIND_BOOTSTRAP_CONFIG_INVALID); peercred native
  module loads; labels verified.
- Compose overlay `docker-compose.blind-public-test.yml`: digest-pinned images,
  host TCP 443 → Edge 9100 direct (no proxy), daemon network_mode none on private
  Unix socket volume, mem/cpu/pids/ulimit limits, `docker compose config` valid.
- Rollback bundle `release/blind-public-test/rollback/` (README + idempotent script):
  stops/removes only the 3 sidecar containers, retains roots, T1 untouched.
- T1 file verified byte-identical & disabled (sha256 bfcc1266…).

## BLOCKING DEFECT FOUND + REMEDIATED (disclosed, escalated)

Accepted-source packaging metadata defect: `packages/blind-ipc/package.json`
`files[]` omits runtime-required accepted source (private-ipc-v3/v4
contract/status + authority v3/v4 + vectors-v3/v4), so `npm ci --install-links`
(npm-pack filtered) images fail at startup with ERR_MODULE_NOT_FOUND
(private-ipc-v3-contract.js, statically re-exported by blind-ipc/index.js since
38cf18e). `packages/blind-edge/package.json` `files[]` omits the accepted
forward-https-vnext.js (not startup-blocking: production cli.js wires server.js;
the vNext edge class is exercised by its accepted test suite). blind-daemon
(`*.js`) / blind-protocol (`*.js`+assets) / blind-peercred are complete.
Remediation (Dockerfile-level, no package metadata touched — outside lane
authority): after `npm ci`, the @hiverelay closure is installed as exact copies
of the accepted source tree (already in the toolchain stage); third-party deps
remain the exact lockfile install; closure isolation re-asserted; build gates on
full module-graph import. Escalated to runtime lane for a files[] amendment; if
independent assurance prefers rebuild-after-amendment over Dockerfile-level
remediation, this evidence supports either path.

## Window 2 (2026-07-24T19:10–19:20Z) — COMPLETE

- trivy 0.72.0 (digest-pinned) scans ×4 (both images × both platforms), full JSON
  reports under `security/`; DB identity recorded (schema 2, UpdatedAt
  2026-07-24T13:17:21Z, DownloadedAt 2026-07-24T18:08:21Z, db file sha256
  c1dfc514…, repo mirror.gcr.io/aquasec/trivy-db:2).
- Disposition written (`security/scan-disposition.json`): zero findings in the
  relay closure /opt/hiverelay; fix-available CRITICAL/HIGH all inside the base
  image's bundled npm CLI (never executed by the runtime) → ACCEPT_RISK;
  no-fix OS CRITICALs (perl-base ×4, zlib1g ×1) → WATCH; gate result
  NO_BLOCKING_FINDING under `security/vulnerability-policy.json`.
- `artifact-manifest.json` generated (UNSIGNED-PENDING-INDEPENDENT-ASSURANCE),
  binding source commit/tree, all image/platform/index digests, SBOM digests,
  provenance subjects, scanner disposition, browser artifact sha256, compose
  overlay sha256, rollback plan, claim boundary.
- Rollback script smoke-tested against the empty project (idempotent, T1 verify OK).
- Lane evidence: assignments/release-and-appliances/evidence/release-artifacts-20260724t181322z.json
- Large artifacts (oci-archives 707M, trivy-cache 1.1G) retained on disk,
  git-ignored, digests bound in the manifest.

## Remaining

- commit A: regenerated browser artifacts (separate commit per lane brief)
- commit B: release-lane tooling (Dockerfiles, compose overlay, release/blind-public-test/**)
- handoff to independent assurance
