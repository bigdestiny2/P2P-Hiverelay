node_modules/
.DS_Store
*.log
.env
.env.*
storage/
*.pid
coverage/
.nyc_output/
dist/
.publisher-storage/
.pos-publisher-storage/
test-zk-storage/
ghost-seed-demo.mjs
*.jpg
*.jpeg
*.png
packages/core/node_modules
tmp-audit2/
tmp-test-relay2/

# Legacy dashboard symlink (some long-lived boxes have packages/core/dashboard ->
# /dashboard; relay serves from repo-root /dashboard regardless). Ignore so the
# orphan never trips the fleet updater's dirty-tree guard.
packages/core/dashboard

# local smoke storage
tmp-test-relay*/

# Private fleet inventory: same shape as fleet/relays.json but with real
# operator SSH key paths. Never commit key paths — the tracked inventory uses
# the "default" sentinel. Consume via HIVERELAY_FLEET_INVENTORY / --relays.
fleet/relays.local.json
