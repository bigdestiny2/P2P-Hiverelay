# Package assets

StartOS 0.4 requires this directory as an `s9pk` build ingredient. Blindspark
does not currently ship additional runtime assets; the service icon and user
instructions remain the top-level `icon.svg` and `instructions.md` package
inputs.
